services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: logitrack-db
    ports:
      - "5432:5432"
    volumes:
        - pgdata:/var/lib/postgresql
volumes:
    pgdata:
          