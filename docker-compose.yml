services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: LogiTrackDb
    ports:
      - "5433:5432"
    volumes:
        - pgdata:/var/lib/postgresql
volumes:
    pgdata:
          